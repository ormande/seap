{
  "name": "licitacao-extrator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "node scripts/run-backend.js",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}